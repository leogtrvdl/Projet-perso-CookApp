package com.CookApp.CookApp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CookAppAppApplication {

	public static void main(String[] args) {
		SpringApplication.run(CookAppAppApplication.class, args);
	}
	//verif route data quand =/ connecter (structure ou données)
	//test des méthodes controller
}